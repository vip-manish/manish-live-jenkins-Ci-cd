<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Todo List App</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: 'Roboto', Arial, sans-serif;
      background: linear-gradient(135deg, #a18cd1, #fbc2eb);
      color: #333;
      margin: 0;
      padding: 0;
    }

    h1 {
      background: linear-gradient(90deg, #6a11cb, #2575fc);
      color: white;
      margin: 0;
      padding: 30px;
      text-align: center;
      font-size: 30px;
    }

    .container {
      max-width: 600px;
      margin: 40px auto;
      padding: 0 20px;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    ul li {
      background: #ffffffcc;
      border-radius: 8px;
      box-shadow: 0 6px 10px rgba(0,0,0,0.15);
      margin-bottom: 15px;
      padding: 15px 50px 15px 15px;
      position: relative;
      transition: transform 0.2s ease;
    }

    ul li:hover {
      transform: translateY(-4px);
    }

    ul li a {
      text-decoration: none;
      font-weight: bold;
    }

    ul li .delete-btn {
      color: #ff1744;
      position: absolute;
      right: 15px;
      top: 15px;
      font-size: 18px;
    }

    ul li .edit-btn {
      color: #ffca28;
      position: absolute;
      right: 45px;
      top: 15px;
      font-size: 18px;
    }

    p {
      text-align: center;
      font-size: 16px;
      color: #444;
    }

    form {
      background: #ffffffcc;
      border-radius: 8px;
      box-shadow: 0 6px 10px rgba(0,0,0,0.15);
      padding: 25px;
      margin-top: 30px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-weight: bold;
      color: #222;
    }

    input[type="text"],
    input[type="url"] {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    input[type="submit"] {
      background: linear-gradient(90deg, #6a11cb, #2575fc);
      color: white;
      border: none;
      padding: 12px 30px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
      font-size: 16px;
      transition: background 0.3s ease;
    }

    input[type="submit"]:hover {
      background: linear-gradient(90deg, #5b0ecb, #1b63e0);
    }

    .todo-image {
      display: block;
      max-width: 100px;
      margin: 10px 0;
      border-radius: 4px;
    }

    hr {
      margin: 40px 0;
      border: none;
      border-top: 2px solid #ddd;
    }

    .poll-form label {
      margin: 0 15px;
      font-weight: normal;
    }

    .poll-form button {
      background: linear-gradient(90deg, #6a11cb, #2575fc);
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
      font-size: 14px;
      transition: background 0.3s ease;
    }

    .poll-form button:hover {
      background: linear-gradient(90deg, #5b0ecb, #1b63e0);
    }
  </style>
</head>

<body>
  <h1>👑 VIP Manish Rathor World<br />Let's Learn CI/CD Pipeline with Manish 🚀</h1>

  <div class="container">
    <ul>
      <% todolist.forEach((todo, index) => { %>
        <li>
          <a href="/todo/<%= index %>" class="edit-btn">✎</a>
          <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
          <%- todo.text %>
          <% if (todo.image) { %>
            <img src="<%= todo.image %>" alt="Todo Image" class="todo-image" />
          <% } %>
        </li>
      <% }); %>
    </ul>

    <p>👨‍💻 My Name is Manish Rathor and this is my First CI/CD Pipeline ❤️</p>

    <form action="/todo/add/" method="POST">
      <label for="newtodo">What should I do?</label>
      <input type="text" name="newtodo" id="newtodo" placeholder="Enter your task..." required />

      <label for="imageurl">Optional Image URL:</label>
      <input type="url" name="imageurl" id="imageurl" placeholder="https://example.com/image.jpg" />

      <input type="submit" value="Add Task" />
    </form>

    <hr />

    <h2 style="text-align:center;">📣 Kaisa laga mera CI/CD project?</h2>
    <form id="feedbackForm" class="poll-form" style="text-align:center;">
      <label>
        <input type="radio" name="feedback" value="Acha" required /> Acha
      </label>
      <label>
        <input type="radio" name="feedback" value="Beka" /> Beka
      </label>
      <label>
        <input type="radio" name="feedback" value="Bahut Badiya" /> Bahut Badiya
      </label>
      <label>
        <input type="radio" name="feedback" value="None of these" /> None of these
      </label>
      <br /><br />
      <button type="submit">Submit Feedback</button>
    </form>

    <p id="feedbackMessage" style="text-align:center; font-weight:bold; margin-top:10px;"></p>
  </div>

  <script>
    const feedbackForm = document.getElementById('feedbackForm');
    const feedbackMessage = document.getElementById('feedbackMessage');

    feedbackForm.addEventListener('submit', function(e) {
      e.preventDefault();
      const selected = document.querySelector('input[name="feedback"]:checked');
      if (selected) {
        feedbackMessage.textContent = `You selected: "${selected.value}" 🙌`;
        feedbackMessage.style.color = '#2575fc';
      }
    });
  </script>
</body>
</html>

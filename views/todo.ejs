<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Todo List App</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', Arial, sans-serif;
      background: linear-gradient(135deg, #e0f7fa, #fff);
      color: #333;
      margin: 0;
      padding: 0;
    }

    h1 {
      background: linear-gradient(90deg, #4CAF50, #8BC34A);
      color: white;
      margin: 0;
      padding: 30px;
      text-align: center;
      font-size: 28px;
    }

    .container {
      max-width: 600px;
      margin: 40px auto;
      padding: 0 20px;
    }

    ul {
      list-style: none;
      padding: 0;
    }

    ul li {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      margin-bottom: 15px;
      padding: 15px 50px 15px 15px;
      position: relative;
      transition: transform 0.2s ease;
    }

    ul li:hover {
      transform: translateY(-2px);
    }

    ul li a {
      text-decoration: none;
      font-weight: bold;
    }

    ul li .delete-btn {
      color: #e53935;
      position: absolute;
      right: 15px;
      top: 15px;
    }

    ul li .edit-btn {
      color: #fbc02d;
      position: absolute;
      right: 45px;
      top: 15px;
    }

    p {
      text-align: center;
      font-size: 16px;
      color: #555;
    }

    form {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      padding: 25px;
      margin-top: 30px;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-weight: bold;
      color: #333;
    }

    input[type="text"],
    input[type="url"] {
      width: 100%;
      padding: 12px;
      margin-bottom: 15px;
      border: 1px solid #ddd;
      border-radius: 4px;
    }

    input[type="submit"] {
      background: #4CAF50;
      color: white;
      border: none;
      padding: 12px 30px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: bold;
      font-size: 16px;
    }

    input[type="submit"]:hover {
      background: #45a049;
    }

    .todo-image {
      display: block;
      max-width: 100px;
      margin: 10px 0;
      border-radius: 4px;
    }
  </style>
</head>

<body>
  <h1>👑 VIP Manish Rathor World<br />Let's Learn CI/CD Pipeline with Manish 🚀</h1>

  <div class="container">
    <ul>
      <% todolist.forEach(function(todo, index) { %>
        <li>
          <a href="/todo/<%= index %>" class="edit-btn">✎</a>
          <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
          <%- todo.text %>
          <% if (todo.image) { %>
            <img src="<%= todo.image %>" alt="Todo Image" class="todo-image" />
          <% } %>
        </li>
      <% }); %>
    </ul>

    <p>👨‍💻 My Name is Manish Rathor and this is my First CI/CD Pipeline ❤️</p>

    <form action="/todo/add/" method="POST">
      <label for="newtodo">What should I do?</label>
      <input type="text" name="newtodo" id="newtodo" placeholder="Enter your task..." required />

      <label for="imageurl">Optional Image URL:</label>
      <input type="url" name="imageurl" id="imageurl" placeholder="https://example.com/image.jpg" />

      <input type="submit" value="Add Task" />
    </form>
  </div>
</body>
</html>
